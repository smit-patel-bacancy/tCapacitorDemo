import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 't-capacitor-demo',
  webDir: 'dist/t-capacitor-demo'
};

export default config;
